plugins {
    id "com.android.application"
}

android {
    namespace "com.kostas.tvaudioplayer"
    compileSdk 35

    defaultConfig {
        applicationId "com.kostas.tvaudioplayer"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        debug {
            minifyEnabled false
        }
        release {
            minifyEnabled false
        }
    }
}
