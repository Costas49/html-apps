plugins {
    id "com.android.application"
}

android {
    namespace "com.kostas.tvaudioplayer"
    compileSdk 35

    defaultConfig {
        applicationId "com.kostas.tvaudioplayer"
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName "2.0"
    }

    buildTypes {
        debug {
            minifyEnabled false
        }
        release {
            minifyEnabled false
        }
    }
}
